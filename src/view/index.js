import Layout from "../layout";
import { BrowserRouter } from "react-router-dom";

const View = () => {
  return (
    <BrowserRouter>
      <Layout />
    </BrowserRouter>
  );
};

export default View;
